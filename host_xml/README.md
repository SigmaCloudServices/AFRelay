# `host_xml/` Folder

This folder contains the XML files used as Docker volumes by the web service:

- loginTicketRequest.xml
- loginTicketResponse.xml

These files are mounted inside the container at `xml_management/app_xml_files/`.

**Never upload these files to GitHub.**